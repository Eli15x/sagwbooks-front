import axios from 'axios'

/*export async function sendEmail(email,name,assunto) { 

    const data = JSON.stringify({ email, name, assunto, telefone});
    let headers = {
      'Content-Type': 'application/json',
    };
  
    try{
      const response = await axios.get('http://localhost:1323/sendEmail', data,headers )
      console.log(response.data)
      return response.data
    }
    catch(err){
      console.log(err)
    }
  
    return ""
  }*/